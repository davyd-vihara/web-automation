# PDF Accessibility Analyzer

Анализатор визуальной доступности PDF файлов с текстом. Проверяет соответствие документа стандартам WCAG 2.1 (уровень AA) и выявляет проблемы с контрастностью, размером шрифта и читаемостью.

## 📦 Установка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd pdf_accessibility_analyzer
```

### 2. Создание виртуального окружения (рекомендуется)
```bash
python -m venv venv
# Для Windows:
venv\Scripts\activate
# Для Mac/Linux:
source venv/bin/activate
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

## 🚀 Использование

### Командная строка (CLI)

Скрипт поддерживает запуск из командной строки с различными опциями:

#### Базовое использование
```bash
# Запуск с файлом по умолчанию (если настроен)
python main.py

# Запуск с указанием файла
python main.py "path/to/document.pdf"

# Запуск с выбором формата отчета
python main.py "document.pdf" --format summary

# Запуск с сохранением в конкретный файл
python main.py "document.pdf" --format json --output report.json

# Запуск со скриншотами
python main.py "document.pdf" --screenshots --screenshot-mode smart
```

#### Доступные опции

| Опция | Короткая | Описание | По умолчанию |
|-------|----------|----------|--------------|
| `pdf_path` | - | Путь к PDF файлу для анализа | Путь из настроек |
| `--format`, `-f` | `-f` | Формат отчета: `full`, `summary`, `statistics`, `json` | `full` |
| `--output`, `-o` | `-o` | Путь для сохранения отчета | Автоматическое имя |
| `--screenshots`, `-s` | `-s` | Создавать скриншоты проблемных областей | `False` |
| `--screenshot-mode` | - | Режим скриншотов: `none`, `area`, `full_page`, `smart` | `smart` |

#### Примеры использования

```bash
# Полный отчет (по умолчанию)
python main.py document.pdf

# Краткий отчет
python main.py document.pdf --format summary

# Только статистика
python main.py document.pdf --format statistics

# JSON формат для программной обработки
python main.py document.pdf --format json -o report.json

# С созданием скриншотов
python main.py document.pdf --screenshots --screenshot-mode full_page

# Просмотр справки
python main.py --help
```

### Программное использование (Python API)

```python
from main import EnhancedPDFAccessibilityAnalyzer

# Инициализация анализатора
analyzer = EnhancedPDFAccessibilityAnalyzer("path/to/your/document.pdf")

# Проведение анализа
issues = analyzer.analyze()

# Генерация отчета
report = analyzer.generate_improved_report(
    output_file="accessibility_report.txt",
    create_screenshots=False,
    screenshot_mode="full_page",
    report_format="full"  # full, summary, statistics, json
)
```

## 📊 Форматы отчетов

### 1. Полный отчет (`full`)
Подробный отчет со всей информацией о проблемах:
- Сводная статистика по серьезности и типам
- Детальный анализ каждого типа проблем
- Группировка по текстовым паттернам
- Обзор по страницам
- Отчет по проблемным цветам
- Рекомендации по исправлению

### 2. Краткий отчет (`summary`)
Сжатая версия с основной информацией:
- Общее количество проблем
- Статистика по серьезности
- Топ-3 типа проблем

### 3. Статистика (`statistics`)
Только числовая статистика без деталей:
- Распределение по типам и серьезности
- Количество мест и слов проблемного текста

### 4. JSON формат (`json`)
Структурированные данные для программной обработки:
- Полная статистика в JSON
- Список проблем (первые 1000)
- Информация о цветовых проблемах

### Пример отчета

```
📊 УЛУЧШЕННЫЙ ОТЧЕТ ПО ДОСТУПНОСТИ PDF
================================================================================

📄 Документ: example.pdf
📈 Всего проблем: 24,366 мест
📊 Всего слов проблемного текста: 195,234
📑 Затронуто страниц: 194

📊 СВОДНАЯ СТАТИСТИКА:
========================================

🟡 MEDIUM: 17,618 мест (154,321 слов)
   • Размер шрифта: 13,063 мест (118,542 слов)
   • Контрастность: 4,163 мест (35,779 слов)

🟢 LOW: 6,748 мест (40,913 слов)
   • Контрастность: 6,748 мест (40,913 слов)
```

## ⚙️ Настройки анализа

### Минимальные требования WCAG 2.1

- **Минимальный размер основного текста:** 12pt
- **Минимальный размер заголовков:** 14pt
- **Минимальная контрастность обычного текста:** 4.5:1
- **Минимальная контрастность крупного текста** (≥18pt или ≥14pt жирный): 3.0:1

### Читаемые шрифты

**Рекомендуемые:**
- Arial, Helvetica, Verdana, Tahoma
- Georgia, Times New Roman
- Open Sans, Roboto, Lato

**Не рекомендуемые:**
- Comic Sans, Papyrus, Brush Script
- Декоративные и моноширинные шрифты

## 📈 Типы обнаруживаемых проблем

### 1. Контрастность
- Низкая контрастность текста на фоне
- Проблемные цвета (серый, светло-зеленый, желтый)
- Несоответствие требованиям WCAG

### 2. Размер шрифта
- Мелкий основной текст (<12pt)
- Мелкие заголовки (<14pt)
- Некорректное определение крупного текста

### 3. Читаемость шрифтов
- Использование плохо читаемых шрифтов
- Декоративные и моноширинные шрифты
- Нестандартные шрифтовые решения

## 🎨 Особенности обработки текста

### Нормализация текста
Инструмент автоматически:
- Удаляет дублированные символы (например, "ГГЛЛООССССААРРИИЙЙ" → "ГЛОССАРИЙ")
- Нормализует текст для группировки
- Удаляет лишние пробелы
- Подсчитывает слова вместо символов для более понятной статистики

### Группировка проблем
- Группировка по текстовым паттернам
- Статистика по страницам
- Распределение по серьезности (high, medium, low)

## 🔧 Доступные методы

### Основные методы анализатора

```python
analyzer.analyze()  # Провести анализ PDF
analyzer.generate_improved_report()  # Сгенерировать отчет
analyzer.create_screenshot()  # Создать скриншот страницы
analyzer.create_full_page_screenshots()  # Создать скриншоты всех страниц
analyzer.count_words()  # Подсчитать слова в тексте
```

### Настройка создания отчетов

```python
analyzer.generate_improved_report(
    output_file="report.txt",           # Путь для сохранения отчета
    create_screenshots=True,            # Создавать ли скриншоты
    screenshot_mode="smart",            # Режим создания скриншотов
    report_format="full"                # Формат отчета
)
```

### Доступные режимы скриншотов

- `"none"` - не создавать скриншоты
- `"area"` - создавать скриншоты проблемных областей
- `"full_page"` - создавать полностаничные скриншоты
- `"smart"` - комбинированный режим

### Доступные форматы отчетов

- `"full"` - полный подробный отчет (по умолчанию)
- `"summary"` - краткий отчет
- `"statistics"` - только статистика
- `"json"` - JSON формат для программной обработки

## 📁 Структура проекта

```
pdf_accessibility_analyzer/
├── main.py                    # Основной файл с анализатором
├── requirements.txt           # Зависимости проекта
├── README.md                  # Документация
├── RECOMMENDATIONS.md         # Рекомендации по улучшению
└── reports/                   # Папка для сохранения отчетов
    └── *.txt, *.json         # Сгенерированные отчеты
```

## 🛠️ Разработка

### Добавление новых проверок

Весь код находится в файле `main.py`. Для добавления новой проверки:

1. Добавьте новый тип проблемы в класс `AccessibilityIssue`
2. Реализуйте проверку в методе `analyze_page()`
3. Обновите генерацию отчетов в `generate_improved_report()`

Пример:
```python
# В методе analyze_page()
if some_condition:
    page_issues.append(AccessibilityIssue(
        page=page_num,
        x=char.get('x0', 0),
        y=char.get('y0', 0),
        text=text_preview,
        issue_type='Новая проверка',
        description='Описание проблемы',
        severity='medium',
        font_name=font_name,
        font_size=font_size
    ))
```

## 🧪 Тестирование

### Создание тестового PDF

Для тестирования можно использовать PDF-документы с:
- Разными размерами шрифта
- Разными цветами текста
- Разными типами шрифтов
- Разным уровнем контрастности

### Проверка работы

```python
# Проверьте, что модуль импортируется
from main import EnhancedPDFAccessibilityAnalyzer, AccessibilityIssue

# Проверьте основные функции
analyzer = EnhancedPDFAccessibilityAnalyzer("test.pdf")
word_count = analyzer.count_words("Пример текста для проверки")
assert word_count == 5  # 5 слов
```

## 📚 Полезные ссылки

- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)
- [PDF Accessibility Overview](https://www.w3.org/WAI/GL/WCAG20-TECHS/pdf.html)
- [Color Contrast Checker](https://webaim.org/resources/contrastchecker/)

## 🐛 Известные ограничения

- **Определение цвета фона** - используется упрощенный алгоритм (предполагается белый фон)
- **Сложные макеты** - могут быть проблемы с анализом текста в таблицах и колонках
- **Сканированные PDF** - не анализируются (только текст, извлеченный с помощью pdfplumber)
- **Шрифты** - определение жирности основано на названии шрифта

## 🔄 Планы по развитию

- [ ] Добавить проверку альтернативного текста для изображений
- [ ] Добавить проверку структуры документа (заголовки, списки)
- [ ] Добавить поддержку сканированных PDF через OCR
- [ ] Добавить графический интерфейс
- [ ] Добавить экспорт отчетов в формате HTML/PDF
- [ ] Добавить API для интеграции с другими системами
- [ ] Разделить код на модули для лучшей организации

## 📞 Поддержка

Если у вас есть вопросы или предложения:

1. Создайте Issue в репозитории
2. Опишите проблему или предложение
3. Приложите пример PDF (если возможно)

## 🙏 Благодарности

- **pdfplumber** - за отличную библиотеку для работы с PDF
- **WCAG** - за стандарты доступности
- Сообществу разработчиков за вклад в open source проекты

## ⚠️ Примечание

Этот инструмент предназначен для помощи в оценке доступности PDF-документов, но не заменяет полную аудит доступности, проводимый экспертами. Всегда тестируйте документы с реальными пользователями с ограниченными возможностями.

## 📝 Лицензия

[Укажите лицензию проекта]

## 🔄 История изменений

### Версия 2.0
- ✅ Добавлен CLI интерфейс с аргументами командной строки
- ✅ Добавлены разные форматы отчетов (full, summary, statistics, json)
- ✅ Подсчет слов вместо символов для более понятной статистики
- ✅ Улучшена обработка ошибок и валидация входных данных
- ✅ Добавлена поддержка пути по умолчанию

### Версия 1.0
- Базовая функциональность анализа доступности
- Проверка контрастности, размера шрифта и читаемости
- Генерация текстовых отчетов
